name: Build and Push Docker Image

on:
  push:
    branches: [ action-merge ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - &push_condition
        if: github.event_name == 'push' && github.ref == 'refs/heads/main'

      - name: Checkout source code
        uses: actions/checkout@v3

      - name: Setup Gradle
        uses: gradle/actions/setup-gradle@v4

      - name: Gradle build
        <<: *push_condition
        run: |
          ./gradlew clean build --no-daemon -x test

#      - name: Set up Docker Buildx
#        uses: docker/setup-buildx-action@v2
#
#      - name: Log in to Docker Hub
#        uses: docker/login-action@v3
#        with:
#          username: ${{ secrets.DOCKER_USERNAME }}
#          password: ${{ secrets.DOCKER_PASSWORD }}
#
#      - name: Build and Push Docker Image
#        if: contains(github.ref, 'main')
#        uses: docker/build-push-action@v6
#        with:
#          context: .
#          push: true
#          tags: cnux9/scheduler:latest
#
#      - name: Deploy to prod
#        if: contains(github.ref, 'main')
#        uses: appleboy/ssh-action@master
#        id: deploy-prod
#        with:
#          host: ${{ secrets.HOST_PROD }}
#          username: ubuntu
#          key: ${{ secrets.PRIVATE_KEY }}
#          script: |
#            sudo docker ps
#            if [ "$(docker ps -a -q -f name=scheduler-container)" ]; then
#              echo "기존 컨테이너 발견. 제거합니다..."
#              docker rm -f scheduler-container
#            else
#              echo "기존 컨테이너 없음."
#            fi
#            sudo docker pull ${{ secrets.DOCKER_USERNAME }}/scheduler:latest
#            docker run -d \
#            --name scheduler-container \
#            --restart no -p 8080:8080 \
#            -e SPRING_DATASOURCE_URL="jdbc:mysql://${{ secrets.RDS_ENDPOINT }}:3306/scheduler" \
#            -e SPRING_DATASOURCE_USERNAME=${{ secrets.RDS_USERNAME }} \
#            -e SPRING_DATASOURCE_PASSWORD=${{ secrets.RDS_PASSWORD }} \
#            ${{ secrets.DOCKER_USERNAME }}/scheduler:latest
#            sudo docker image prune -f
